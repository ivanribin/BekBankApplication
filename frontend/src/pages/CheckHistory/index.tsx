import { type ReactElement } from "react";
import BankLogo from "../../components/BankLogo";

const CheckHistoryPage = (): ReactElement => (
    <div className="check-history-page-container">
        <BankLogo />
        <p className="page-header">Check History Page</p>
    </div>
)

export default  CheckHistoryPage;