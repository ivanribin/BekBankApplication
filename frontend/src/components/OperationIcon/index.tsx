import { type ReactElement } from "react";
import "./style.css"

const OperationIcon = (): ReactElement => (
    <div className="operation-icon-container">
        <img src="/assets/images/operationIcon.png" alt="operation icon" />
    </div>
)

export default OperationIcon;